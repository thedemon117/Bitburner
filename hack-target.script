// Defines the "target server", which is the server
// by passing the first agrument to it - hack-target.script [server name]
var target = args[0]
// Infinite loop that continously hacks/grows/weakens the target server
while(true) {
        hack(target);
        sleep(120000);
}